// Deprecado — conexión COM ahora es global en App.tsx (useWebSerialAllflex).
// Se mantiene el archivo por compatibilidad; no renderiza nada.
export default function AllflexScanner() {
    return null;
}
